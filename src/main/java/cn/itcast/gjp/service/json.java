package cn.itcast.gjp.service;




/**
 * @author apple
 * @date 2019/10/31 上午11:53
 */
public class json {

    /**
     * 判断字符串是否可以转化为json对象
     * @param content
     * @return
     */
    /*public static boolean isJsonObject(String content) {

        if(StringUtils.isBlank(content))

            return false;
        try {
            JSONObject jsonStr = JSONObject.paraseO
                    parseObject(content);
            return true;
        } catch (Exception e) {
            return false;
        }
    }*/
}
